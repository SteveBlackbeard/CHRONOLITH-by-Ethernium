name: CONTINUITY LEGACY Validation & Release

on:
  push:
    branches: ["**"]
    tags: ["v*"]
  pull_request:
    branches: ["**"]

jobs:
  validate-continuity:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Run Continuity Legacy Cycle
        run: |
          cd "CONTINUITY LEGACY Pro"
          python tools/continuity_legacy/run_continuity_cycle.py --repo-root . --strict

  pypi-release:
    name: Publish to PyPI
    if: startsWith(github.ref, 'refs/tags/v')
    needs: validate-continuity
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Build Packages
        run: |
          python -m pip install --upgrade build
          cd continuity-legacy-pypi
          python -m build
          cd ../continuity-legacy-lite-pypi
          python -m build
          cd ..

      - name: Publish PRO to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: continuity-legacy-pypi/dist/

      - name: Publish LITE to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: continuity-legacy-lite-pypi/dist/
