# Change Contract: Release 3.2.2

## Summary
Version 3.2.1 -> 3.2.2 across VERSION and the four pyproject.toml, plus the
golden baseline that governs them.

## Scope
- VERSION, pyproject.toml (x4), CHANGELOG.md, .chronolith/golden-baseline.json
Out of scope: runtime behaviour. The fixes this release ships were reviewed and
merged already; the bump only changes the number.

## Reason
Ships the audit fixes an agent found using the tool: check now fails closed by
default (C-1), severity is respected, the pre-push hook is portable (C-3), the
status panel matches the exit code (C-6), .chronolithignore excludes non-
canonical trees (C-4), init is honest on re-run (C-5), --quiet exists (C-8),
docs reference real commands (C-7), and descriptions say what each package does.

## Risk
Low. Metadata bump plus reviewed fixes, all covered by regression tests.

## Verification
```text
python scripts/audit_all.py
python scripts/golden_baseline.py verify
python scripts/health_guard.py --strict
```
Publication is verified after the fact by the PyPI attestation naming this
repository and workflow.

## Rollback
```text
git revert <commit>
```
